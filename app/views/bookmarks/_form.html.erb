<%= form_with(model: bookmark, local: true) do |form| %>
  <% if bookmark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bookmark.errors.count, "error") %> prohibited this bookmark from being saved:</h2>

      <ul>
      <% bookmark.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="field">
    <%= form.label :subcategory_id %>
    <%= form.collection_select(:subcategory_id, Subcategory.all, :id, :name, prompt:true) %>
  </div>

  <div class="field">
    <%= form.label :type_id %>
    <%= form.collection_select(:type_id, Category.all, :id, :name, prompt:true) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
